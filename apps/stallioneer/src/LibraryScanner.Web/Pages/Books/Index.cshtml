@page
@model LibraryScanner.Web.Pages.Books.IndexModel
@{
    ViewData["Title"] = "Inventory";
}

<section class="page-heading">
    <div>
        <p class="eyebrow">Personal collection</p>
        <h1>Inventory</h1>
    </div>
    <div class="header-actions">
        <a class="btn btn-outline-secondary" asp-page="/Collections/Index">Collections</a>
        <a class="btn btn-outline-secondary" asp-page="/Tags/Index">Tags</a>
        <a class="btn btn-outline-secondary" asp-page="/Locations/Index">Locations</a>
        <a class="btn btn-primary" asp-page="/Books/Create">Add book</a>
    </div>
</section>

@if (!string.IsNullOrWhiteSpace(Model.StatusMessage))
{
    <div class="alert alert-success">@Model.StatusMessage</div>
}

<section class="stats-grid compact-stats-grid">
    <div class="stat">
        <span class="stat-label">Titles</span>
        <strong>@Model.TotalTitles</strong>
    </div>
    <div class="stat">
        <span class="stat-label">Total items</span>
        <strong>@Model.TotalQuantity</strong>
    </div>
    <div class="stat">
        <span class="stat-label">Tags</span>
        <strong>@Model.TotalTags</strong>
    </div>
    <div class="stat">
        <span class="stat-label">Collections</span>
        <strong>@Model.TotalCollections</strong>
    </div>
</section>

<form method="get" class="toolbar inventory-filters">
    <input asp-for="Query" class="form-control" placeholder="Search ISBN, title, author, publisher, tags, collections, location, notes" />
    <select asp-for="Sort" asp-items="Model.SortOptions" class="form-select"></select>
    <select asp-for="TagId" asp-items="Model.TagOptions" class="form-select"></select>
    <select asp-for="CollectionId" asp-items="Model.CollectionOptions" class="form-select"></select>
    <select asp-for="PageSize" asp-items="Model.PageSizeOptions" class="form-select"></select>
    <button class="btn btn-outline-secondary" type="submit">Search</button>
    <a class="btn btn-outline-secondary" asp-page="/Books/Index">Clear</a>
</form>

@if (Model.Books.Count == 0)
{
    <div class="empty-state">
        <h2>No books found</h2>
        <p>Add a book or clear the search to see the full inventory.</p>
        <a class="btn btn-primary" asp-page="/Books/Create">Add book</a>
    </div>
}
else
{
    <form method="post" class="bulk-form">
        <input asp-for="Query" type="hidden" />
        <input asp-for="Sort" type="hidden" />
        <input asp-for="TagId" type="hidden" />
        <input asp-for="CollectionId" type="hidden" />
        <input asp-for="PageSize" type="hidden" />
        <input asp-for="PageNumber" type="hidden" />
        <section class="panel bulk-panel">
            <div class="bulk-actions">
                <div class="bulk-copy">
                    <strong>Selected books</strong>
                    <span class="form-hint">Showing @Model.Books.Count of @Model.FilteredCount matching books. Delete selected after confirmation, or add them to a custom collection.</span>
                </div>
                <select asp-for="SelectedCollectionId" asp-items="Model.CollectionOptions.Skip(1)" class="form-select">
                    <option value="">Choose collection</option>
                </select>
                <button asp-page-handler="AddToCollection" class="btn btn-outline-secondary">Add to collection</button>
                <button asp-page-handler="DeleteSelected" class="btn btn-outline-danger" onclick="return confirm('Delete the selected books? This cannot be undone.');">Delete selected</button>
            </div>
        </section>

        <div class="table-wrap">
            <table class="table inventory-table">
                <thead>
                    <tr>
                        <th>
                            <a class="sort-link @(Model.IsSortedBy("title") ? "is-active" : string.Empty)" asp-page="/Books/Index" asp-route-query="@Model.Query" asp-route-sort="@Model.GetNextSort("title")" asp-route-tagId="@Model.TagId" asp-route-collectionId="@Model.CollectionId" asp-route-pageSize="@Model.PageSize">@($"Book{Model.GetSortIndicator("title")}")</a>
                        </th>
                        <th>
                            <a class="sort-link @(Model.IsSortedBy("isbn") ? "is-active" : string.Empty)" asp-page="/Books/Index" asp-route-query="@Model.Query" asp-route-sort="@Model.GetNextSort("isbn")" asp-route-tagId="@Model.TagId" asp-route-collectionId="@Model.CollectionId" asp-route-pageSize="@Model.PageSize">@($"ISBN{Model.GetSortIndicator("isbn")}")</a>
                        </th>
                        <th>
                            <a class="sort-link @(Model.IsSortedBy("quantity") ? "is-active" : string.Empty)" asp-page="/Books/Index" asp-route-query="@Model.Query" asp-route-sort="@Model.GetNextSort("quantity")" asp-route-tagId="@Model.TagId" asp-route-collectionId="@Model.CollectionId" asp-route-pageSize="@Model.PageSize">@($"Qty{Model.GetSortIndicator("quantity")}")</a>
                        </th>
                        <th>
                            <a class="sort-link @(Model.IsSortedBy("location") ? "is-active" : string.Empty)" asp-page="/Books/Index" asp-route-query="@Model.Query" asp-route-sort="@Model.GetNextSort("location")" asp-route-tagId="@Model.TagId" asp-route-collectionId="@Model.CollectionId" asp-route-pageSize="@Model.PageSize">@($"Location{Model.GetSortIndicator("location")}")</a>
                        </th>
                        <th>
                            <a class="sort-link @(Model.IsSortedBy("tag") ? "is-active" : string.Empty)" asp-page="/Books/Index" asp-route-query="@Model.Query" asp-route-sort="@Model.GetNextSort("tag")" asp-route-tagId="@Model.TagId" asp-route-collectionId="@Model.CollectionId" asp-route-pageSize="@Model.PageSize">@($"Tags{Model.GetSortIndicator("tag")}")</a>
                        </th>
                        <th>
                            <a class="sort-link @(Model.IsSortedBy("collection") ? "is-active" : string.Empty)" asp-page="/Books/Index" asp-route-query="@Model.Query" asp-route-sort="@Model.GetNextSort("collection")" asp-route-tagId="@Model.TagId" asp-route-collectionId="@Model.CollectionId" asp-route-pageSize="@Model.PageSize">@($"Collections{Model.GetSortIndicator("collection")}")</a>
                        </th>
                        <th>
                            <a class="sort-link @(Model.IsSortedBy("status") ? "is-active" : string.Empty)" asp-page="/Books/Index" asp-route-query="@Model.Query" asp-route-sort="@Model.GetNextSort("status")" asp-route-tagId="@Model.TagId" asp-route-collectionId="@Model.CollectionId" asp-route-pageSize="@Model.PageSize">@($"Status{Model.GetSortIndicator("status")}")</a>
                        </th>
                        <th class="selection-column">
                            <input type="checkbox" data-select-all aria-label="Select all books" />
                        </th>
                    </tr>
                </thead>
                <tbody>
                @foreach (var book in Model.Books)
                {
                    var titleAuthor = string.IsNullOrWhiteSpace(book.Authors) ? book.Title : $"{book.Title} - {book.Authors}";
                    <tr>
                        <td>
                            <div class="book-cell">
                                @if (!string.IsNullOrWhiteSpace(book.CoverImageUrl))
                                {
                                    <img src="@book.CoverImageUrl" alt="" />
                                }
                                else
                                {
                                    <div class="cover-placeholder"></div>
                                }
                                <div>
                                    <div class="copy-line">
                                        <a class="book-title-link" asp-page="/Books/Edit" asp-route-id="@book.Id"><strong>@book.Title</strong></a>
                                        <button type="button" class="copy-button text-copy-button" data-copy-text="@titleAuthor" title="Copy title and author" aria-label="Copy title and author">Copy</button>
                                    </div>
                                    <span>@book.Authors</span>
                                    @if (!string.IsNullOrWhiteSpace(book.Publisher))
                                    {
                                        <small>@book.Publisher @book.PublishedDate</small>
                                    }
                                </div>
                            </div>
                        </td>
                        <td>
                            <div class="copy-line">
                                <span>@book.Isbn13</span>
                                <button type="button" class="copy-button text-copy-button" data-copy-text="@book.Isbn13" title="Copy ISBN" aria-label="Copy ISBN">Copy</button>
                            </div>
                        </td>
                        <td>@book.EffectiveQuantity</td>
                        <td>@book.Location?.Name</td>
                        <td>
                            <div class="tag-list">
                                @foreach (var bookTag in book.BookTags.OrderBy(bookTag => bookTag.Tag.Name))
                                {
                                    <span class="tag-pill" style="--tag-color:@bookTag.Tag.Color">@bookTag.Tag.Name</span>
                                }
                            </div>
                        </td>
                        <td>
                            <div class="tag-list">
                                @foreach (var collectionBook in book.CollectionBooks.OrderBy(item => item.Collection.Name))
                                {
                                    <span class="status-pill collection-pill">@collectionBook.Collection.Name</span>
                                }
                            </div>
                        </td>
                        <td><span class="status-pill">@book.Status</span></td>
                        <td class="selection-column">
                            <input type="checkbox" name="SelectedBookIds" value="@book.Id" data-select-item aria-label="Select @book.Title" />
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </div>

        @if (Model.TotalPages > 1)
        {
            <div class="pager">
                <span class="form-hint">Page @Model.PageNumber of @Model.TotalPages</span>
                <div class="pager-actions">
                    @if (Model.PageNumber > 1)
                    {
                        <a class="btn btn-outline-secondary" asp-page="/Books/Index" asp-route-query="@Model.Query" asp-route-sort="@Model.Sort" asp-route-tagId="@Model.TagId" asp-route-collectionId="@Model.CollectionId" asp-route-pageSize="@Model.PageSize" asp-route-pageNumber="@(Model.PageNumber - 1)">Previous</a>
                    }
                    @if (Model.PageNumber < Model.TotalPages)
                    {
                        <a class="btn btn-outline-secondary" asp-page="/Books/Index" asp-route-query="@Model.Query" asp-route-sort="@Model.Sort" asp-route-tagId="@Model.TagId" asp-route-collectionId="@Model.CollectionId" asp-route-pageSize="@Model.PageSize" asp-route-pageNumber="@(Model.PageNumber + 1)">Next</a>
                    }
                </div>
            </div>
        }
    </form>
}
