import type { Mode, QualityLevel } from '../types';

interface StatusOverlayProps {
  mode: Mode;
  quality: QualityLevel;
}

export function StatusOverlay({ mode: _mode, quality: _quality }: StatusOverlayProps) {
  return null;
}
